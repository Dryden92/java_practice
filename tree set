import java.util.TreeSet;
public class Main
{
	public static void main(String[] args) 
	{
	    TreeSet<Object> set=new TreeSet<Object>();
	    
	    set.add(10);   //insertion not maintain but gives sorted array
	    set.add(20);   //hetrogeneous not allowed
	    set.add(30);   //null value not allowed
	    set.add(50);   //default size is 0
	    set.add(60);   //duplicate not allowed
	    set.add(10);   //not synchronized
	    set.add(40);
	    
	    System.out.println(set.isEmpty());
	    System.out.println(set.contains(10));
	    System.out.println(set.size());
		System.out.println(set);
		
	}
}
